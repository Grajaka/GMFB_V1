module.exports = {
  content: ['./index.html', './kitchen-sink.html'],
  theme: {
    extend: {
      //
    },
  },
  //plugins: [require('./src')],
  plugins: [
      require('@tailwindcss/forms')
      ({
        strategy: 'class';// Only generates form-* classes, no global base styles
      }),
  ],
}
